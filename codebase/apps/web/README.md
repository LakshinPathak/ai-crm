# Frontend — Next.js

UI for AI CRM. Talks to `codebase/backend` at `NEXT_PUBLIC_API_URL`.

```bash
pnpm dev          # from repo root, or: pnpm dev here
pnpm test:e2e     # Playwright E2E (requires API :4000 + web :3000 running)
```
