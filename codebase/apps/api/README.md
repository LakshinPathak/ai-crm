# Backend — Express + MongoDB

Node.js API for AI CRM. All routes under `/api/v1`.

```bash
pnpm dev          # from repo root, or: pnpm dev here
```

**Auth:** Google OAuth + JWT. Dev fallback: `POST /api/v1/auth/dev-login`.

See `docs/api-routes.md` for module build order.
